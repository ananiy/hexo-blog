// Jenkinsfile (Declarative Pipeline)

pipeline {
    agent { docker 'node:8-alipne' }
    stages {
        stage('build') {
            steps {
                sh 'npm --version'
            }
        }
    }
}
